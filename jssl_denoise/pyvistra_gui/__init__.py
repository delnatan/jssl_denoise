"""pyvistra plugin GUI for jssl_denoise -- train + denoise dialogs.

Deliberately empty: submodules import Qt/torch lazily (see
``jssl_denoise/_pyvistra_plugin.py``), so importing this package at
``import jssl_denoise`` time stays cheap and Qt-free.
"""
